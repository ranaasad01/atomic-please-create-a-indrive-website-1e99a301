import type { Metadata } from "next";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";
import LocaleProvider from "@/components/LocaleProvider";
import LanguageToggle from "@/components/LanguageToggle";

export const metadata: Metadata = {
  formatDetection: { telephone: false, date: false, email: false, address: false },
  title: "inDrive — Negotiate Your Fare",
  description:
    "inDrive is a ride-hailing platform where riders and drivers negotiate fares directly. Get a ride, deliver packages, or earn money driving on your own terms.",
  keywords: ["ride-hailing", "inDrive", "fare negotiation", "driver", "delivery"],
  openGraph: {
    title: "inDrive — Negotiate Your Fare",
    description:
      "Ride on your terms. Negotiate fares directly with drivers. Available in 700+ cities worldwide.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous" />
        <link
          href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&family=Inter:wght@300;400;500;600&display=swap"
          rel="stylesheet"
        />
      </head>
      <body className="bg-[#1A1A1A] text-white antialiased font-body">
        <LocaleProvider>
          <LanguageToggle />
          <Navbar />
          <main>{children}</main>
          <Footer />
        </LocaleProvider>
      </body>
    </html>
  );
}